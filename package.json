{
  "name": "ai-concept-lab-3d-lab-002-retail-flow-simulator",
  "version": "2.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "dev": "node --watch --env-file-if-exists=.env server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs && node --test tests/*.test.mjs",
    "render:instagram": "node marketing/render.mjs"
  },
  "devDependencies": {
    "@resvg/resvg-js": "2.6.2"
  },
  "engines": {
    "node": ">=22"
  }
}
